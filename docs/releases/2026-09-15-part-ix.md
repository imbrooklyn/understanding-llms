# Part IX publication — September 15, 2026

## Authorization and scope

Following completion and validation of Part IX, the user requested publication. This release promotes only Chapters 54–57 in English and Chinese: eight reading pages with `published: 2026-09-15`. Chapters 1–4 retain September 10, and Chapters 5–39 plus Primer-PY retain September 13. Chapters 40–53 and Primer-HTTP remain completed web drafts; the multimodal extension remains an unpublished placeholder. The explicit release inventory is `data/releases/2026-09-15-publication.json`.

The source commit includes the completed, previously uncommitted prerequisite project and validation assets from Parts VII/VIII because the Part IX service, frozen manifests, capstone comparisons and CI depend on them. Their chapter publication flags are not promoted. Existing draft prose, code, source lines, notebooks, licenses and measured records are preserved byte for byte. The companion service is a local CPU demonstration; this release does not deploy its Python server or expose order operations on the public website.

## Publication changes

- Promote the eight target pages with matching dates. Chapters 55 and 56 change metadata only. Chapters 54 and 57 additionally turn links to unpublished web prerequisites into plain references and add a bilingual publication note pointing to the source repository. Their technical explanations, values, figures and exercise answers are unchanged.
- Update bilingual home pages, roadmap and changelog, using the configured chapter titles. The gap between the published foundational sequence and Part IX is explicit. Prerequisite draft text and code remain available as source material; this release does not claim to replace that learning path.
- Replace Part IX's draft-only assertions with the existing valid-publication-state checks. Retain every content, calculation, behavior, evidence-hash and notebook assertion. Add an independent release-inventory test that checks all 60 chapter/primer/extension identifiers, both languages, unchanged previous dates and exclusion of every unpromoted page.
- Extend production browser checks to the actual available roadmap and to English/Chinese search opening Chapter 57. Keep all existing Part IX desktop/mobile, light/dark, no-JavaScript and companion-service checks.
- Use the established website-owned GitHub Pages workflow. Its already prepared Python 3.12.10 setup and pinned behavior-check dependencies are necessary for the current book CI. No alternate host, site redesign, PDF or EPUB is introduced.

## Actual local verification

The publication environment was macOS 26.5.2 arm64, Node 23.10.0, pnpm 11.24.0, Python 3.12.10 in `/tmp/llm-book-part-ix-venv`, Astro 7.3.1 and Playwright 1.63.0 with Chrome 153.0.8010.36. `BOOK_PYTHON` selected that clean Python environment. These publication checks do not rerun neural training or rewrite the prior measured evidence.

| Check | Actual result |
| --- | --- |
| Book `BOOK_PYTHON=/tmp/llm-book-part-ix-venv/bin/python pnpm ci:build` | Passed all content/language/Part I–IX validators, CPU behavior suites, existing Node tests, Astro check, production build and output filtering |
| Added release-inventory test, followed by `pnpm test` | All 26 Node tests passed; prior dates and all remaining drafts checked in both languages |
| Book production output | 101 HTML pages, including 100 indexed language pages; each locale feed contains 43 chapters with source dates; all 32 draft language routes and links excluded |
| Main-site `pnpm ci:build` and `pnpm compose:site` | Passed; all 324 book files matched the combined copy byte for byte |
| Combined link/asset/fragment inspection | 10,232 same-site references across 107 HTML pages resolved; zero missing targets or fragments |
| Production browser suite | 34/34 passed, zero skips or failures, 45.1 seconds |
| Working-tree whitespace check | `git diff --check` passed for modified tracked files |

The browser command was:

```sh
BOOK_PYTHON=/tmp/llm-book-part-ix-venv/bin/python \
BOOK_RELEASE_CHECK=1 \
BOOK_PREVIEW_URL=http://127.0.0.1:4326 \
pnpm test:browser tests/part-ix.browser.ts tests/part-ix-service.browser.ts tests/release.browser.ts \
  --output test-results/release-20260915 --reporter=list
```

The preview was an owned loopback Python static server exposing the actual combined artifact. The tests cover both languages at 1440×1000 and 390×844, all eight chapter pages, figures in both themes, readable formulas and answers without JavaScript, same-chapter language switching, local overflow and links. Release cases check the home pages, all 43 roadmap chapter links, the Python primer, all excluded routes and real search opening Chapter 57. The six companion-service cases launch their own local instances and exercise observable UI behavior.

Actual production home, roadmap and search captures were visually reviewed in addition to automated assertions. Figure sources and engineering code were unchanged by publication; their preceding complete visual and CPU/notebook evidence remains in `docs/part-ix-validation.md`. Publication captures and command logs remain local verification artifacts, distinct from the archived draft-authoring screenshots.

A pre-publication hash inventory covers 802 existing files. Comparison confirms that the previously completed project, data, notebooks, figures and author records retain their bytes. The only additional body text in target chapters is the publication note and corresponding removal of unavailable web links. The source commit includes existing work without resetting it. Historical author records retain their original draft statements and hashes; this release supersedes their publication status, not their measurements.

The complete staged commit contains 370 files, including previously uncommitted dependencies and evidence. Its whitespace check reports existing raw-log trailing spaces and blank EOF lines in frozen practice files. Every reported file matches its pre-publication hash; those bytes were retained rather than invalidating historical measurements. Publication-edited files pass the targeted whitespace check. This distinction does not turn the whole staged whitespace report into a passing check.

## Deployment and limits

The book repository is the source of truth. The [website Pages workflow](https://github.com/imbrooklyn/imbrooklyn.github.io/actions/workflows/deploy.yml) records the exact book revision it builds and owns production deployment. The local results above precede deployment; the workflow run is the authoritative completion record. Reading starts at [Chapter 54 in Chinese](https://imbrooklyn.dev/books/understanding-llms/zh-hans/ch-54/) or [Chapter 54 in English](https://imbrooklyn.dev/books/understanding-llms/en/ch-54/).

Publishing the textbook does not enlarge its empirical claims. The assistant retains known lexical misses, failed earlier model-selection evidence, small evaluation denominators and the documented limits of local fault drills, access control, feedback retention and configuration rollback. No production service load claim, full accessibility conformance, new neural benchmark or independent security certification is made. The research, cards, risk owners and run/rollback instructions remain part of the evidence package.
