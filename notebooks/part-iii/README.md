# Part III notebooks

Execute in order after the Python primer. `01-state-and-gradient.ipynb` accompanies Chapters 11–12; `02-seq2seq-attention.ipynb` accompanies Chapters 13–14. Every core result and reference answer is also statically available in both chapter languages. No interactive learning widget is used by the website.

Use the isolated CPU setup and runner in `code/part-iii/README.md`. Both notebooks must execute from a fresh kernel, with sequential execution counts and no cell errors. The second includes an optional NumPy recomputation, which is included in the recorded full execution. Outputs, code hashes, imported dependency hashes, interpreter version, and elapsed times make that execution inspectable.

Original implementation: Apache-2.0. Original explanatory prose and supplied teaching data: CC BY-SA 4.0. No third-party notebook, model, tokenizer, or corpus is copied.
