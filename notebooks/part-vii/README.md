# Part VII notebooks

Run from the book repository root in the [declared CPU environment](../../code/part-vii/README.md). Every code cell is executed in order by `pnpm notebooks:part-vii`; an opened Notebook alone is not execution evidence.

1. `01-contracts-tools.ipynb`: KA-2 context/schema and KA-3 read-only Tool, HTTP and MCP. Read the Python Primer first and the HTTP Primer before network cells.
2. `02-retrieval-rag.ipynb`: retained same-query rankings, independent arithmetic, a fully traced local extractive RAG path, layered evaluation and separate ablations.

Original code cells: Apache-2.0. Original explanatory text and fictional fixture content: CC BY-SA 4.0. Saved outputs are actual local executions with producer labels. No real credential or remote model output is stored. The handwritten dense map is not trained; the NNLM vectors are inherited learned artifacts; the answer composer is not a neural generator.
