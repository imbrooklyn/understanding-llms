# Chapter 10 figure sources

Both figures are original bilingual content resources, CC BY-SA 4.0. `window-lm.json` corresponds to the fully supplied arithmetic in `data/part-ii/nnlm-paper.json`, not the trained run. `outside-window.json` uses the separately declared five-symbol diagnostic in `nnlm-data.json`; it demonstrates identical visible contexts and different required targets without pretending those words belong to the trained KA vocabulary. No third-party artwork or pretrained vectors are redistributed.
