# Chapter 34 figure

`target-mask.json` is an original static computation diagram, CC BY-SA 4.0. Both languages share the same structure and numbers. Its assigned probabilities and independently enumerated targets come from `data/part-vi/sft-v1.json` and `expected.json`. It contains no third-party imagery and depicts no measured training run.
