# Chapter 7 figure sources

Both JSON files are shared bilingual content resources, original and CC BY-SA 4.0. `one-update.json` represents independently derived arithmetic, reproduced by PyTorch. `training-curve.json` supplies localized labels and descriptions; `book-curves.mjs` reads all 601 actual train/validation observations from `data/part-ii/classifier-run.json`. Linear axes are update count and mean nats per example. Line styles distinguish series without relying on color. The test split is absent from the selection curve. No third-party visual material is embedded.
