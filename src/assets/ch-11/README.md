# Chapter 11 figure sources

`unrolled.json` is the shared bilingual source for Figure 11.1. Original design and computed teaching values, CC BY-SA 4.0. It uses the existing static lane renderer; neutral double borders denote parameters and vector nodes denote hidden activations. No paper figure is reproduced. Values are defined in `data/part-iii/sequence-v1.json`.
