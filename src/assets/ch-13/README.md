# Chapter 13 figure sources

`encoder-decoder.json` displays computed encoder states and an explicitly required target path; `target-timelines.json` shows both supplied-conditional-table traces. Neither figure claims a fitted decoder.

Both JSON sources contain English and Simplified Chinese content and reuse the static lane renderer. Original design, CC BY-SA 4.0; no third-party figure is copied. Shared numerical fixtures: `data/part-iii/sequence-v1.json`. Parameter boxes use neutral double borders; hidden states use vector nodes.
