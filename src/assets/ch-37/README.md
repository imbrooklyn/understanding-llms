# Chapter 37 figure

`budget-curve.json` and its static SVG renderer are original. Figure content and fixed candidate data use CC BY-SA 4.0; renderer code uses Apache-2.0. The curve reads `data/part-vi/budget-v1.json` and is independently checked against controller execution. It plots the declared call cap and correct-question count in primary order, not actual model accuracy, tokens or elapsed time. The complete numeric table is also visible in each manuscript. Both languages and themes use line/marker distinctions without relying on color.
