# Chapter 14 figure sources

`two-reads.json` calculates both unscaled dot-product reads in one shared bilingual source. `remaining-path.json` marks source and target recurrence and the narrower parallel-comparison opportunity. Original diagrams, CC BY-SA 4.0. Queries are authored probes; encoder states are computed. No paper figures or measured model activations are reproduced. Both use the existing static lane renderer and work without JavaScript. Values: `data/part-iii/sequence-v1.json`.
